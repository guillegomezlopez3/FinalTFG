import 'package:flutter/material.dart';

/// Paleta de colores oficial de la aplicación LevelUp.
/// 
/// Centraliza todos los valores cromáticos para mantener la consistencia visual.
class AppColors {
  /// Color primario (Naranja).
  static const Color primary = Color(0xFFFF5722);
  
  /// Versión clara del color primario.
  static const Color primaryLight = Color(0xFFFF7043);
  
  /// Versión oscura del color primario.
  static const Color primaryDark = Color(0xFFE64A19);

  /// Color de fondo de la aplicación.
  static const Color background = Color(0xFFF0F4F8);
  
  /// Color de fondo para tarjetas y contenedores elevados.
  static const Color cardBg = Color(0xFFFFFFFF);

  /// Fondo oscuro para barras laterales o menús.
  static const Color sidebarBg = Color(0xFF0F1623);
  
  /// Color intermedio para degradados o separadores oscuros.
  static const Color sidebarMid = Color(0xFF1A2235);
  
  /// Color de texto para elementos sobre fondos oscuros.
  static const Color sidebarText = Color(0xFF8B95A5);

  /// Color principal para el texto del cuerpo.
  static const Color text = Color(0xFF1A1D2E);
  
  /// Color para texto secundario o menos importante.
  static const Color textMuted = Color(0xFF6B7280);

  /// Color para bordes y separadores claros.
  static const Color border = Color(0xFFE8ECF0);
}

