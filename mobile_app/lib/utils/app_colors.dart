import 'package:flutter/material.dart';

class AppColors {
  // Brand colors
  static const Color primary = Color(0xFFFF5722);        // --orange
  static const Color primaryLight = Color(0xFFFF7043);   // --orange-light
  static const Color primaryDark = Color(0xFFE64A19);    // --orange-dark

  // Background and UI
  static const Color background = Color(0xFFF0F4F8);     // --bg
  static const Color cardBg = Color(0xFFFFFFFF);         // --card-bg

  // Sidebar / Dark Elements
  static const Color sidebarBg = Color(0xFF0F1623);      // --sidebar-bg
  static const Color sidebarMid = Color(0xFF1A2235);     // --sidebar-mid
  static const Color sidebarText = Color(0xFF8B95A5);    // --sidebar-text

  // Text
  static const Color text = Color(0xFF1A1D2E);           // --text
  static const Color textMuted = Color(0xFF6B7280);      // --text-muted

  // Borders
  static const Color border = Color(0xFFE8ECF0);         // --border
}

